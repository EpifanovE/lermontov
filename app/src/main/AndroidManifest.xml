<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.eecode.lermontov">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="ru.eecode.lermontov.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Dir">

        <activity
            android:name="ru.eecode.lermontov.ui.StartActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Dir.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="ru.eecode.lermontov.ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Dir.NoActionBar" >
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4504008939187380~1390327850"/>

    </application>

</manifest>